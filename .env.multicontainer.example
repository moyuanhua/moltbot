# ===============================================
# Moltbot 多容器环境变量配置示例
# Multi-Container Environment Configuration
# ===============================================

# ===== Gateway 配置 =====
# Gateway Token: 用于身份验证的密钥（必填）
# 生成方式: openssl rand -hex 32
CLAWDBOT_GATEWAY_TOKEN=<请填写生成的64位hex token>

# Gateway 端口: Gateway HTTP 服务监听端口
CLAWDBOT_GATEWAY_PORT=18789

# Gateway 绑定: 监听地址
#   - loopback: 仅本地访问 (127.0.0.1)
#   - lan: 局域网访问 (0.0.0.0)
CLAWDBOT_GATEWAY_BIND=lan

# Bridge 端口: WebSocket 桥接端口（如需要）
CLAWDBOT_BRIDGE_PORT=18790

# ===== 目录配置 =====
# 配置目录: 存储 Moltbot 配置文件和会话数据
# 默认: ~/.clawdbot
CLAWDBOT_CONFIG_DIR=/root/.clawdbot

# 工作区目录: Agent 工作区
# 默认: ~/clawd
CLAWDBOT_WORKSPACE_DIR=/root/clawd

# ===== 镜像配置 =====
# Gateway 镜像名称
CLAWDBOT_IMAGE=moltbot:local

# ===== Claude API 配置（可选）=====
# 如果使用 Claude AI 官方 API，填写以下配置

# Claude AI Session Key
CLAUDE_AI_SESSION_KEY=

# Claude Web Session Key
CLAUDE_WEB_SESSION_KEY=

# Claude Web Cookie
CLAUDE_WEB_COOKIE=

# ===== 网络配置 =====
# 注意: moltbot-internal 网络已在 docker-compose.multicontainer.yml 中定义
# 无需在此配置

# ===== 使用说明 =====
# 1. 复制此文件为 .env.multicontainer
#    cp .env.multicontainer.example .env.multicontainer
#
# 2. 生成 Gateway Token
#    openssl rand -hex 32
#
# 3. 编辑 .env.multicontainer，填写必要配置
#
# 4. 启动服务
#    docker-compose -f docker-compose.multicontainer.yml --env-file .env.multicontainer up -d
#
# 或使用部署脚本（推荐）
#    ./deploy-multicontainer.sh
